export enum GlobalEnum {
  //映射分隔符
  MAPPING_SEPARATOR = '<-->',

  MAX_PAGE_SIZE = 500,

  PAGE_SIZE = 10,

  SLASH_SEPARATOR = ' / ',

  KCIM_REPOSITORY_ID = 1,
}

export enum ModelType {
  REPOSITORY = 1,
  FIELD = 2,
  BIZ = 3,
  TABLE = 4,
  COLUMN = 5,
}
